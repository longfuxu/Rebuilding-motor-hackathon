# macOS
.DS_Store

# Python (for local ringperm-gp16 workspaces if created inside the repo)
.venv/
__pycache__/
*.pyc

# Secrets — never commit
.env

# large regenerable MD trajectories (see SESSION_LOG.md to regenerate)
*.dcd
